---
// src/pages/sensores.astro
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
---

<Layout title="Sensores - Sistema IoT">
  <Navigation />
  
  <main class="px-4 py-6 pb-20">
    <div class="text-center mb-6">
      <div class="text-5xl mb-3">📊</div>
      <h1 class="text-2xl md:text-3xl font-bold text-gray-900 mb-2">
        Sensores en Tiempo Real
      </h1>
      <p class="text-sm text-gray-600">
        Datos actualizados automáticamente
      </p>
    </div>

    <!-- Estado de conexión -->
    <div class="max-w-lg mx-auto mb-6">
      <div id="connectionStatus" class="bg-yellow-50 border border-yellow-200 rounded-lg p-3 text-center">
        <span class="text-yellow-700 text-sm font-medium">⏳ Conectando...</span>
      </div>
    </div>

    <!-- Cards de sensores -->
    <div class="space-y-4 max-w-lg mx-auto">
      <!-- Temperatura -->
      <div class="bg-gradient-to-br from-red-50 to-orange-50 p-5 rounded-xl shadow-md">
        <div class="flex justify-between items-center mb-3">
          <div class="flex items-center space-x-3">
            <span class="text-3xl">🌡️</span>
            <h3 class="font-semibold text-gray-800">Temperatura</h3>
          </div>
          <span class="text-xs text-gray-500">Actualizado hace 2s</span>
        </div>
        <div class="text-4xl font-bold text-gray-900" id="temperatura">
          --°C
        </div>
      </div>

      <!-- Humedad -->
      <div class="bg-gradient-to-br from-blue-50 to-cyan-50 p-5 rounded-xl shadow-md">
        <div class="flex justify-between items-center mb-3">
          <div class="flex items-center space-x-3">
            <span class="text-3xl">💧</span>
            <h3 class="font-semibold text-gray-800">Humedad</h3>
          </div>
          <span class="text-xs text-gray-500">Actualizado hace 2s</span>
        </div>
        <div class="text-4xl font-bold text-gray-900" id="humedad">
          --%
        </div>
      </div>

      <!-- Luz -->
      <div class="bg-gradient-to-br from-yellow-50 to-amber-50 p-5 rounded-xl shadow-md">
        <div class="flex justify-between items-center mb-3">
          <div class="flex items-center space-x-3">
            <span class="text-3xl">💡</span>
            <h3 class="font-semibold text-gray-800">Luz</h3>
          </div>
          <span class="text-xs text-gray-500">Actualizado hace 2s</span>
        </div>
        <div class="text-4xl font-bold text-gray-900" id="luz">
          -- lux
        </div>
      </div>

      <!-- Movimiento -->
      <div class="bg-gradient-to-br from-purple-50 to-pink-50 p-5 rounded-xl shadow-md">
        <div class="flex justify-between items-center mb-3">
          <div class="flex items-center space-x-3">
            <span class="text-3xl">🏃</span>
            <h3 class="font-semibold text-gray-800">Movimiento</h3>
          </div>
          <span class="text-xs text-gray-500">Actualizado hace 2s</span>
        </div>
        <div class="text-2xl font-bold text-gray-900" id="movimiento">
          Sin detectar
        </div>
      </div>
    </div>

    <!-- Info -->
    <div class="max-w-lg mx-auto mt-6 bg-blue-50 p-4 rounded-lg">
      <p class="text-xs text-gray-600 text-center">
        ℹ️ Los datos se actualizan automáticamente desde Firebase
      </p>
    </div>
  </main>
</Layout>

<script>
  // Aquí irá la lógica de Firebase para leer sensores
  // Por ahora solo simulamos datos
  console.log('Página de sensores cargada');
  
  // Simular conexión exitosa
  setTimeout(() => {
    const status = document.getElementById('connectionStatus');
    if (status) {
      status.className = 'bg-green-50 border border-green-200 rounded-lg p-3 text-center';
      status.innerHTML = '<span class="text-green-700 text-sm font-medium">✓ Conectado</span>';
    }
  }, 1000);
</script>